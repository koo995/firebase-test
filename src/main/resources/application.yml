spring:
  application:
    name: firebase
  datasource:
    url: jdbc:h2:tcp://localhost/~/firetest
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true


firebase:
  credential:
    resource-path: firebase.json